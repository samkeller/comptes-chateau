import { PageTemplate } from "../PageTemplate";

export default function Index() {
    return (
        <PageTemplate pageTitle="Accueil">
            <div className="text-surface-500">Page d'accueil en attente.</div>
        </PageTemplate>
    );
}