1. Un seul composant `SelectNatureDropdown` pour toute l'application
    - props :
        - value
        - changeValue
    - options calculées dans le composant
    - allowNull par défaut (voir conséquences)

2. Un seul composant `SelectPosteDropdown` pour toute l'application
    - props :
        - value
        - changeValue
    - options calculées dans le composant
    - allowNull par défaut (voir conséquences)